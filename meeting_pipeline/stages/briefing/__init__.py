"""
stages.briefing — Meeting briefing generation.

Takes normalized meeting JSON + constituent data and generates
a multi-pass briefing (categorize → cards → details → validate).

Entry point: process_one_meeting()
"""
